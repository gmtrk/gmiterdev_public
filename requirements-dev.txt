# Development & test tooling. Pulls in the production deps too, so a single
#   pip install -r requirements-dev.txt
# gives you a complete local environment. (`make install` does this for you.)
-r requirements.txt

ruff>=0.6
pytest>=8.0
pytest-django>=4.8
